<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gerenciar Usuários</title>
    <link rel="shortcut icon" href="../static/images/logo.png" type="image/x-icon">
    <link rel="stylesheet" href="/static/css/list_user.css">
    <link rel="stylesheet" href="/static/css/dashboard.css">
</head>
<body>
    <nav class="sidebar">
        <a href="/" class="logo">
            <img src="../static/images/logo.png" alt="Logo">
        </a>
        <a href="/home">Início</a>
        <a href="/perfil">Meu Perfil</a>
        <a href="/prova/gerar">Gerar Prova</a>
        <a href="/users" class="active">Gerenciar Usuários</a>
        <a href="/logout" class="logout-btn">Sair</a>
    </nav>

    <div class="main">
        <header class="topbar">
            <span class="welcome-text">Bem-vindo, <%= user.nome %>!</span>
            <img src="/static/uploads/<%= user.avatar_url || 'user.png' %>" alt="Avatar do Usuário" class="avatar">
        </header>
        <main class="dashboard-content">
            <div class="user-list-container">
                <h1>Gerenciar Usuários</h1>

                <div class="filter-controls">
                    <form action="/users" method="GET">
                        <input type="text" name="search" placeholder="Buscar por nome ou e-mail" value="<%= search %>">
                        <button type="submit">Buscar</button>
                    </form>
                    <div class="status-filter">
                        <a href="/users?status=ativos&search=<%= search %>" class="<%= status === 'ativos' ? 'active' : '' %>">Ativos</a>
                        <a href="/users?status=inativos&search=<%= search %>" class="<%= status === 'inativos' ? 'active' : '' %>">Inativos</a>
                        <a href="/users?status=todos&search=<%= search %>" class="<%= status === 'todos' ? 'active' : '' %>">Todos</a>
                    </div>
                </div>

                <% if (users && users.length > 0) { %>
                    <table class="user-table">
                        <thead>
                            <tr>
                                <th>ID</th>
                                <th>Nome</th>
                                <th>E-mail</th>
                                <th>Role</th>
                                <th>Status</th>
                                <th>Ações</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% users.forEach(u => { %>
                                <tr>
                                    <td><%= u.id_usuario %></td>
                                    <td><%= u.nome %></td>
                                    <td><%= u.email %></td>
                                    <td><%= u.role %></td>
                                    <td><%= u.ativo ? 'Ativo' : 'Inativo' %></td>
                                    <td class="actions">
                                        <a href="/users/<%= u.id_usuario %>/editar" class="edit">Editar</a>
                                        <% if (u.ativo) { %>
                                            <form action="/users/<%= u.id_usuario %>/delete" method="POST" style="display:inline;">
                                                <button type="submit" class="delete" onclick="return confirm('Tem certeza que deseja desativar este usuário?');">Desativar</button>
                                            </form>
                                        <% } else { %>
                                            <form action="/users/<%= u.id_usuario %>/reactivate" method="POST" style="display:inline;">
                                                <button type="submit" class="reactivate" onclick="return confirm('Tem certeza que deseja reativar este usuário?');">Reativar</button>
                                            </form>
                                        <% } %>
                                    </td>
                                </tr>
                            <% }); %>
                        </tbody>
                    </table>
                <% } else { %>
                    <p class="no-users">Nenhum usuário encontrado com os critérios de busca.</p>
                <% } %>
            </div>
        </main>
    </div>
</body>
</html>