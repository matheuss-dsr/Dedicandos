<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Dedicandos</title>
    <link rel="shortcut icon" href="../static/images/logo.png" type="image/x-icon">
    <link rel="stylesheet" href="/static/css/login-cadastro.css">
    <style>
        .error, .success {
            padding: 10px;
            margin-bottom: 15px;
            border-radius: 5px;
            text-align: center;
            font-weight: bold;
        }
        .error {
            background-color: #f8d7da;
            color: #721c24;
            border: 1px solid #f5c6cb;
        }
        .success {
            background-color: #d4edda;
            color: #155724;
            border: 1px solid #c3e6cb;
        }
    </style>
</head>
<body>
    <div class="auth-container">
        <h2>Login</h2>

        <!-- Verificação defensiva: só renderiza se error existir e não for null/undefined -->
        <% if (typeof error !== 'undefined' && error !== null && error) { %>
            <div class="error"><%= error %></div>
        <% } %>
        <!-- Verificação defensiva: só renderiza se success existir e não for null/undefined -->
        <% if (typeof success !== 'undefined' && success !== null && success) { %>
            <div class="success"><%= success %></div>
        <% } %>

        <form action="/login" method="POST">
            <div class="form-group">
                <label for="email">E-mail:</label>
                <input type="email" id="email" name="email" required>
            </div>
            <div class="form-group">
                <label for="senha">Senha:</label>
                <input type="password" id="senha" name="senha" required>
            </div>
            <button type="submit" class="btn-primary">Entrar</button>
        </form>
        <div class="auth-links">
            <a href="/esqueci-senha">Esqueci minha senha</a></br>
            Não tem uma conta? <a href="/cadastro">Cadastre-se</a>
        </div>
    </div>
</body>
</html>